# Портфолио Сайт

Современный портфолио сайт с впечатляющими анимациями и интерактивными элементами.

## Особенности

- ✨ Плавные анимации при скролле
- 🎨 Современный дизайн с градиентами
- 📱 Полностью адаптивный дизайн
- 🎯 Интерактивные элементы
- 🌊 Параллакс эффекты
- 💫 Анимированный фон
- ⌨️ Анимация печатающего текста
- 📊 Анимированные счетчики
- 🎭 Эффекты при наведении
- 🌍 Многоязычность (Русский, Английский, Немецкий)

## Технологии

- HTML5
- CSS3 (с анимациями и градиентами)
- Vanilla JavaScript
- Google Fonts (Inter)

## Установка и запуск

1. Клонируйте репозиторий или скачайте файлы
2. Откройте `index.html` в браузере
3. Или используйте локальный сервер:

```bash
# С помощью Python
python -m http.server 8000

# С помощью Node.js (http-server)
npx http-server

# С помощью PHP
php -S localhost:8000
```

Затем откройте браузер и перейдите по адресу `http://localhost:8000`

## Структура проекта

```
portfol/
├── index.html      # Основная HTML структура
├── styles.css      # Стили и анимации
├── script.js       # JavaScript для интерактивности
├── config.js       # Конфигурация для формы обратной связи
└── README.md       # Документация
```

## Настройка

### Изменение цветов

Откройте `styles.css` и измените переменные в `:root`:

```css
:root {
    --primary-color: #6366f1;    /* Основной цвет */
    --secondary-color: #8b5cf6; /* Вторичный цвет */
    --accent-color: #ec4899;     /* Акцентный цвет */
}
```

### Изменение текста

Отредактируйте содержимое в `index.html`:
- Имя и описание в секции Hero
- Информацию о себе в секции About
- Навыки в секции Skills
- Проекты в секции Projects
- Контактную информацию в секции Contact

### Изменение анимации печатающего текста

В `script.js` найдите объект `translations` и измените тексты в секции `typing.texts` для каждого языка:

```javascript
translations.ru.typing.texts = ['Разработчик', 'Дизайнер', 'Креативщик'];
translations.en.typing.texts = ['Developer', 'Designer', 'Creative'];
translations.de.typing.texts = ['Entwickler', 'Designer', 'Kreativer'];
```

### Добавление новых языков

Чтобы добавить новый язык:

1. Добавьте новый объект в `translations` в `script.js`
2. Скопируйте структуру из существующего языка (например, `ru`)
3. Переведите все тексты
4. Добавьте кнопку языка в HTML:
```html
<button class="lang-btn" data-lang="xx">XX</button>
```

Выбранный язык автоматически сохраняется в localStorage браузера.

## Настройка формы обратной связи

Форма обратной связи поддерживает отправку сообщений на Gmail или в Telegram. Для настройки откройте файл `config.js`.

### Вариант 1: Отправка на Gmail (рекомендуется для email)

📧 **Подробная пошаговая инструкция:** См. файл `GMAIL_SETUP.md`

**Краткая инструкция:**
1. Зарегистрируйтесь на [EmailJS.com](https://www.emailjs.com/)
2. Подключите Gmail как Email Service
3. Создайте Email Template с вашим Gmail адресом
4. Скопируйте Service ID, Template ID и Public Key
5. Заполните их в файле `config.js`

### Вариант 2: Отправка в Telegram

1. **Создайте Telegram бота:**
   - Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
   - Отправьте команду `/newbot`
   - Следуйте инструкциям и получите токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

2. **Узнайте свой Chat ID:**
   - Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
   - Отправьте ему любое сообщение
   - Он вернет ваш Chat ID (число, например `123456789`)

3. **Настройте config.js:**
   ```javascript
   telegram: {
       enabled: true,
       botToken: 'ВАШ_ТОКЕН_БОТА',
       chatId: 'ВАШ_CHAT_ID'
   },
   useMethod: 'telegram'
   ```

### Вариант 3: Отправка в оба места

Установите `useMethod: 'both'` и включите оба сервиса в `config.js`.

## Браузерная поддержка

- Chrome (последние версии)
- Firefox (последние версии)
- Safari (последние версии)
- Edge (последние версии)

## Лицензия

Свободное использование для личных и коммерческих проектов.

