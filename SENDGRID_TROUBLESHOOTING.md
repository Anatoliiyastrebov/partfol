# 🔍 Диагностика отправки email через SendGrid

## ❌ Проблема: Письма не приходят на email

### Шаг 1: Проверьте логи на Render

1. Зайдите в **Render Dashboard**: https://dashboard.render.com
2. Выберите ваш сервис `portfolio-backend`
3. Перейдите в **Logs**

#### ✅ Что должно быть при успешной отправке:

```
📧 Получен запрос на отправку сообщения
📝 Полученные данные: {...}
📤 Отправка email через SendGrid...
✅ Email успешно отправлен через SendGrid!
   Status Code: 202
   Получатель: ваш_email@gmail.com
   Отправитель: ваш_email@gmail.com
```

#### ❌ Если видите ошибки:

**Ошибка 1: "The from address does not match a verified Sender Identity"**

**Решение:**
1. Перейдите в SendGrid Dashboard: https://app.sendgrid.com/settings/sender_auth/senders
2. Убедитесь, что `EMAIL_FROM` добавлен как verified sender
3. Если нет - нажмите **"Create New Sender"**
4. Заполните форму и подтвердите email (проверьте почту!)

**Ошибка 2: "Invalid API key"**

**Решение:**
1. Проверьте, что `SENDGRID_API_KEY` начинается с `SG.`
2. Убедитесь, что скопировали полный ключ (обычно ~70 символов)
3. Проверьте, что ключ имеет права "Mail Send" или "Full Access"
4. Создайте новый API ключ, если нужно

**Ошибка 3: "Forbidden" или "401 Unauthorized"**

**Решение:**
1. Проверьте правильность `SENDGRID_API_KEY`
2. Убедитесь, что ключ не истёк
3. Создайте новый API ключ

---

### Шаг 2: Проверьте переменные окружения на Render

Render Dashboard → ваш сервис → **Environment**

#### Обязательные переменные:

1. **SENDGRID_API_KEY**
   - ✅ Должен начинаться с `SG.`
   - ✅ Должен быть полным (обычно ~70 символов)
   - ✅ Должен иметь права "Mail Send"

2. **EMAIL_FROM**
   - ✅ Должен быть verified sender в SendGrid
   - ✅ Формат: `your-email@gmail.com` (без кавычек)

3. **EMAIL_TO**
   - ✅ Email, куда приходят письма
   - ✅ Может быть тот же, что и `EMAIL_FROM`

#### ⚠️ Важно:

- После изменения переменных **обязательно сделайте Manual Deploy**!
- Render Dashboard → Deployments → ⋯ → **Manual Deploy**

---

### Шаг 3: Проверьте SendGrid Dashboard

#### 3.1. Проверьте Verified Sender

1. Перейдите: https://app.sendgrid.com/settings/sender_auth/senders
2. Убедитесь, что ваш email есть в списке
3. Статус должен быть **"Verified"** (зелёная галочка)
4. Если статус **"Unverified"** - проверьте почту и перейдите по ссылке подтверждения

#### 3.2. Проверьте Activity (активность отправки)

1. Перейдите: https://app.sendgrid.com/activity
2. Там должны быть записи о попытках отправки
3. Если видите ошибки - кликните на них для деталей

#### 3.3. Проверьте API Keys

1. Перейдите: https://app.sendgrid.com/settings/api_keys
2. Убедитесь, что ваш API ключ активен
3. Проверьте права доступа (должно быть "Mail Send" или "Full Access")

---

### Шаг 4: Проверьте почту

#### 4.1. Проверьте папку "Входящие"

#### 4.2. Проверьте папку "Спам" / "Junk"

Письма от SendGrid могут попадать в спам, особенно при первом использовании.

#### 4.3. Проверьте правильность EMAIL_TO

Убедитесь, что в Render указан правильный email в переменной `EMAIL_TO`.

---

### Шаг 5: Тестирование через SendGrid Dashboard

1. Перейдите: https://app.sendgrid.com/mail
2. Нажмите **"Send a Single Email"**
3. Заполните:
   - **From:** ваш verified sender email
   - **To:** ваш email
   - **Subject:** Test
   - **Content:** Test message
4. Нажмите **"Send"**
5. Если письмо пришло - значит SendGrid работает, проблема в коде
6. Если письмо не пришло - проблема в настройках SendGrid

---

### Шаг 6: Проверьте лимиты SendGrid

#### Free план SendGrid:
- **100 писем в день** бесплатно
- Если превышен лимит - письма не будут отправляться

#### Проверка:
1. SendGrid Dashboard → **Settings** → **Billing**
2. Проверьте, сколько писем отправлено сегодня

---

## 🔧 Быстрая диагностика

### Чек-лист:

- [ ] В логах Render видно "✅ Email успешно отправлен через SendGrid!"
- [ ] Status Code: 202 (успех)
- [ ] `SENDGRID_API_KEY` начинается с `SG.` и полный
- [ ] `EMAIL_FROM` является verified sender в SendGrid
- [ ] `EMAIL_TO` указан правильно
- [ ] После изменения переменных сделан Manual Deploy
- [ ] Проверена папка Спам в почте
- [ ] В SendGrid Activity есть записи о попытках отправки
- [ ] Не превышен лимит 100 писем/день (Free план)

---

## 🧪 Тестирование через curl

Проверьте backend напрямую:

```bash
curl -X POST https://portfolio-backend-db2d.onrender.com/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "message": "Test message from curl"
  }'
```

Должен вернуться:
```json
{
  "success": true,
  "message": "Сообщение успешно отправлено",
  "statusCode": 202
}
```

Затем проверьте логи на Render - должны быть сообщения об отправке.

---

## 📊 Проверка в SendGrid Activity

1. Зайдите в SendGrid Dashboard: https://app.sendgrid.com/activity
2. Найдите последние попытки отправки
3. Кликните на запись для деталей

**Статусы:**
- ✅ **Delivered** - письмо доставлено
- ⏳ **Processed** - обрабатывается
- ❌ **Bounced** - отскочило (неверный email)
- ❌ **Blocked** - заблокировано (spam, unverified sender)
- ❌ **Dropped** - отклонено (spam, invalid)

---

## ❌ Частые проблемы и решения

### Проблема 1: "The from address does not match a verified Sender Identity"

**Причина:** `EMAIL_FROM` не verified в SendGrid

**Решение:**
1. https://app.sendgrid.com/settings/sender_auth/senders
2. Create New Sender
3. Заполните форму
4. Подтвердите email

### Проблема 2: Письма в спаме

**Причина:** Первые письма от SendGrid часто попадают в спам

**Решение:**
1. Проверьте папку Спам
2. Отметьте письмо как "Не спам"
3. Добавьте отправителя в контакты

### Проблема 3: "Invalid API key"

**Причина:** Неправильный или истёкший API ключ

**Решение:**
1. Создайте новый API ключ в SendGrid
2. Обновите `SENDGRID_API_KEY` в Render
3. Сделайте Manual Deploy

### Проблема 4: Status Code 202, но письма нет

**Причина:** SendGrid принял письмо, но не доставил

**Решение:**
1. Проверьте SendGrid Activity
2. Проверьте папку Спам
3. Убедитесь, что `EMAIL_TO` правильный
4. Проверьте, что не превышен лимит

---

## 📞 Если ничего не помогает

1. **Проверьте логи на Render** - там будет точная ошибка
2. **Проверьте SendGrid Activity** - там видно статус отправки
3. **Создайте новый API ключ** и verified sender
4. **Проверьте, что все переменные обновлены** и сделан Manual Deploy
5. **Проверьте папку Спам** в почте

---

## ✅ Правильная конфигурация

```
SENDGRID_API_KEY = SG.abcdefghijklmnopqrstuvwxyz1234567890ABCDEFGHIJKLMNOPQRSTUVWXYZ
EMAIL_FROM = ваш_email@gmail.com (verified в SendGrid!)
EMAIL_TO = ваш_email@gmail.com
```

После настройки письма должны приходить на email, указанный в `EMAIL_TO`.
