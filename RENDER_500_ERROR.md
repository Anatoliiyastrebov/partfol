# 🔧 Исправление ошибки 500 на Render

## ❌ Проблема: Ошибка 500 при отправке формы

Ошибка 500 означает, что на сервере произошла ошибка. Нужно проверить логи на Render.

---

## 🔍 Шаг 1: Проверьте логи на Render

1. **Render Dashboard**: https://dashboard.render.com
2. Выберите ваш сервис `portfolio-backend`
3. Перейдите в **Logs**

### Что искать в логах:

**При отправке формы должны быть логи:**
```
📧 Получен запрос на отправку сообщения
📝 Полученные данные: {...}
📤 Отправка email через SendGrid...
```

**Если видите ошибки:**
```
❌ Ошибка при отправке email через SendGrid: ...
```

**Скопируйте точную ошибку из логов** - это поможет определить проблему.

---

## ❌ Частые причины ошибки 500

### Причина 1: Отсутствуют переменные окружения

**Ошибка в логах:**
```
❌ ОШИБКА: Отсутствуют обязательные переменные окружения
```

**Решение:**
1. Render Dashboard → ваш сервис → **Environment**
2. Убедитесь, что все переменные добавлены:
   - `SENDGRID_API_KEY`
   - `EMAIL_FROM`
   - `EMAIL_TO`

### Причина 2: Неправильный SENDGRID_API_KEY

**Ошибка в логах:**
```
Invalid API key
401 Unauthorized
```

**Решение:**
1. Проверьте, что `SENDGRID_API_KEY` начинается с `SG.`
2. Убедитесь, что скопировали полный ключ
3. Создайте новый API ключ в SendGrid, если нужно

### Причина 3: EMAIL_FROM не verified

**Ошибка в логах:**
```
The from address does not match a verified Sender Identity
```

**Решение:**
1. Если используете Domain Authentication - убедитесь, что домен verified
2. Если используете Single Sender - убедитесь, что email verified
3. Проверьте в SendGrid Dashboard → Sender Authentication

### Причина 4: Неправильный формат EMAIL_FROM

**Ошибка в логах:**
```
Invalid email address
```

**Решение:**
1. Проверьте формат `EMAIL_FROM`:
   - ✅ Правильно: `contact@anatolii-yastrebov.top`
   - ❌ Неправильно: `contact@anatolii-yastrebov.top.` (лишняя точка)
   - ❌ Неправильно: ` contact@anatolii-yastrebov.top` (лишний пробел)

---

## 🔧 Шаг 2: Проверьте переменные окружения на Render

Render Dashboard → ваш сервис → **Environment**

### Обязательные переменные:

1. **SENDGRID_API_KEY**
   - Значение: `SG.ваш_полный_ключ` (начинается с SG.)
   - Длина: обычно ~70 символов

2. **EMAIL_FROM**
   - Значение: `contact@anatolii-yastrebov.top` (или ваш verified email)
   - Формат: правильный email адрес

3. **EMAIL_TO**
   - Значение: `ваш_email@gmail.com` (куда приходят письма)
   - Формат: правильный email адрес

### После изменения переменных:

**ВАЖНО:** Сделайте **Manual Deploy**:
- Render Dashboard → Deployments → ⋯ → **Manual Deploy**

---

## 🔍 Шаг 3: Проверьте логи при запуске сервера

В логах Render при запуске должно быть:

```
✅ SendGrid настроен
📧 Отправитель: contact@anatolii-yastrebov.top
📬 Получатель: ваш_email@gmail.com
⚠️  Для нормальной доставляемости нужна Domain Authentication в SendGrid!
🚀 Сервер запущен!
```

**Если видите ошибки при запуске:**
- Проверьте переменные окружения
- Убедитесь, что все переменные добавлены

---

## 🧪 Шаг 4: Тестирование через curl

Проверьте backend напрямую:

```bash
curl -X POST https://portfolio-backend-db2d.onrender.com/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "message": "Test message"
  }'
```

**Что возвращается?** (скопируйте ответ)

---

## 📋 Чек-лист диагностики

- [ ] Проверены логи на Render (какая точная ошибка?)
- [ ] Все 3 переменные окружения добавлены (SENDGRID_API_KEY, EMAIL_FROM, EMAIL_TO)
- [ ] SENDGRID_API_KEY правильный (начинается с SG., полный ключ)
- [ ] EMAIL_FROM правильный формат (email адрес)
- [ ] После изменения переменных сделан Manual Deploy
- [ ] Проверены логи при запуске сервера (нет ошибок?)
- [ ] Протестировано через curl (что возвращается?)

---

## 🔗 Полезные ссылки

- **Render Dashboard**: https://dashboard.render.com
- **SendGrid API Keys**: https://app.sendgrid.com/settings/api_keys
- **SendGrid Sender Authentication**: https://app.sendgrid.com/settings/sender_auth

---

## ❓ Что проверить сейчас

1. **Какая точная ошибка в логах Render?** (скопируйте текст ошибки)
2. **Все ли переменные окружения добавлены?** (SENDGRID_API_KEY, EMAIL_FROM, EMAIL_TO)
3. **Что показывает curl запрос?** (если тестировали)

Сообщите точную ошибку из логов Render - это поможет точно определить проблему.
