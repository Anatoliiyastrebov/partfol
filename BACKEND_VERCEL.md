# 🔧 Подключение Backend на Vercel - Пошаговая инструкция

## Проблема
Backend не работает на Vercel, потому что Vercel - это статический хостинг. Нужно использовать Vercel Serverless Functions.

## ✅ Решение уже создано!

Создана serverless function в `api/contact.js`. Теперь нужно только настроить переменные окружения.

---

## 📋 ШАГ 1: Настройка переменных окружения в Vercel

### 1.1. Войдите в Vercel Dashboard
https://vercel.com/dashboard

### 1.2. Выберите ваш проект

### 1.3. Перейдите в Settings → Environment Variables

### 1.4. Добавьте 5 переменных (для каждого окружения):

#### Переменная 1: SMTP_HOST
- **Key:** `SMTP_HOST`
- **Value:** `smtp.gmail.com`
- **Environment:** ✅ Production, ✅ Preview, ✅ Development

#### Переменная 2: SMTP_PORT
- **Key:** `SMTP_PORT`
- **Value:** `587`
- **Environment:** ✅ Production, ✅ Preview, ✅ Development

#### Переменная 3: SMTP_USER
- **Key:** `SMTP_USER`
- **Value:** `ваш_email@gmail.com` (например, `anatoliiyastrebov@gmail.com`)
- **Environment:** ✅ Production, ✅ Preview, ✅ Development

#### Переменная 4: SMTP_PASS
- **Key:** `SMTP_PASS`
- **Value:** `ваш_пароль_приложения_gmail` (16 символов без пробелов)
- **Environment:** ✅ Production, ✅ Preview, ✅ Development

#### Переменная 5: EMAIL_TO
- **Key:** `EMAIL_TO`
- **Value:** `ваш_email@gmail.com` (куда приходят письма)
- **Environment:** ✅ Production, ✅ Preview, ✅ Development

---

## 📧 ШАГ 2: Получение пароля приложения Gmail

### 2.1. Включите двухфакторную аутентификацию
1. Перейдите: https://myaccount.google.com/security
2. Включите "Двухэтапная аутентификация"

### 2.2. Создайте пароль приложения
1. Перейдите: https://myaccount.google.com/apppasswords
2. Выберите "Почта"
3. Выберите "Другое устройство"
4. Введите название: "Vercel Portfolio"
5. Нажмите "Создать"
6. **Скопируйте 16-значный пароль** (например: `abcd efgh ijkl mnop`)
7. **Используйте БЕЗ пробелов** в Vercel: `abcdefghijklmnop`

---

## 🚀 ШАГ 3: Передеплой проекта

### Вариант А: Через веб-интерфейс Vercel
1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите на три точки (⋯)
4. Выберите **"Redeploy"**
5. Подтвердите

### Вариант Б: Через Git
```bash
git commit --allow-empty -m "Redeploy with env vars"
git push
```

**ВАЖНО:** После добавления переменных ОБЯЗАТЕЛЬНО сделайте редеплой!

---

## ✅ ШАГ 4: Проверка работы

### 4.1. Откройте ваш сайт на Vercel

### 4.2. Откройте консоль браузера (F12 → Console)

### 4.3. Заполните и отправьте форму

### 4.4. Проверьте логи:

**В консоли браузера должно быть:**
```
🔗 API Base URL: Относительный путь (/api)
📤 Отправка запроса на: /api/contact
📝 Тело запроса: {...}
📥 Статус ответа: 200
✅ Сообщение успешно отправлено!
```

**В Vercel Dashboard → Functions → contact → Logs должно быть:**
```
📧 Получен запрос на отправку сообщения
📝 Полученные данные: {...}
📤 Отправка email...
✅ Email успешно отправлен!
```

---

## 🔍 Проверка логов в Vercel

1. Зайдите в Vercel Dashboard
2. Выберите проект
3. Перейдите в **Functions**
4. Найдите функцию `contact`
5. Нажмите на неё
6. Перейдите в **Logs**
7. Там будут все логи выполнения

---

## ❌ Troubleshooting

### Проблема: "Сервер не настроен"
**Причина:** Не добавлены переменные окружения или не сделан редеплой

**Решение:**
1. Проверьте, что все 5 переменных добавлены
2. Убедитесь, что переменные добавлены для всех окружений
3. Сделайте редеплой проекта

### Проблема: "Ошибка при отправке email"
**Причина:** Неправильные SMTP настройки или пароль

**Решение:**
1. Проверьте правильность `SMTP_HOST` (должно быть `smtp.gmail.com`)
2. Проверьте правильность `SMTP_PORT` (должно быть `587`)
3. Убедитесь, что используете **пароль приложения**, а не обычный пароль
4. Проверьте логи в Vercel Dashboard → Functions

### Проблема: "Failed to fetch" или CORS ошибка
**Причина:** Проблема с запросом к API

**Решение:**
1. Проверьте, что функция `api/contact.js` существует
2. Проверьте логи в Vercel Dashboard
3. Убедитесь, что проект передеплоен после изменений

### Проблема: Переменные не работают
**Причина:** Переменные не применены к текущему деплою

**Решение:**
1. Убедитесь, что переменные добавлены для **всех окружений**
2. **Обязательно сделайте редеплой** после добавления переменных
3. Проверьте, что в переменных нет лишних пробелов

---

## 📝 Структура проекта для Vercel

```
portfol/
├── api/
│   └── contact.js          ← Serverless function (backend)
├── index.html              ← Frontend
├── styles.css              ← Стили
├── script.js               ← Frontend JS (обновлен для Vercel)
├── package.json            ← Зависимости (nodemailer)
├── vercel.json             ← Конфигурация Vercel
└── ...
```

---

## 🧪 Тестирование локально

Для локального тестирования с Vercel:

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите
vercel login

# Запустите локальный сервер
vercel dev
```

Это запустит локальный сервер с поддержкой serverless functions.

---

## 📊 Проверка через Vercel CLI

```bash
# Просмотр переменных
vercel env ls

# Добавление переменной
vercel env add SMTP_HOST production
# Введите значение: smtp.gmail.com
```

---

## ✅ Чек-лист подключения backend:

- [ ] Все 5 переменных окружения добавлены в Vercel
- [ ] Переменные добавлены для всех окружений (Production, Preview, Development)
- [ ] Используется пароль приложения Gmail (не обычный пароль)
- [ ] Проект передеплоен после добавления переменных
- [ ] Файл `api/contact.js` существует
- [ ] Файл `vercel.json` настроен правильно
- [ ] Файл `package.json` содержит `nodemailer` в dependencies

---

## 🎯 Быстрая проверка

После настройки откройте консоль браузера (F12) и отправьте форму. Должно быть:

✅ `📤 Отправка запроса на: /api/contact`
✅ `📥 Статус ответа: 200`
✅ `✅ Сообщение успешно отправлено!`

Если видите ошибки - проверьте логи в Vercel Dashboard → Functions.
