# Исправление формы обратной связи

## Что было исправлено:

### 1. Улучшена обработка ошибок
- Добавлены подробные логи в консоль
- Улучшены сообщения об ошибках для пользователя
- Правильная обработка сетевых ошибок

### 2. Исправлен CORS на backend
- Теперь разрешены запросы с localhost
- Добавлена поддержка разных источников

### 3. Улучшена валидация
- Проверка всех полей перед отправкой
- Валидация формата email
- Проверка минимальной длины сообщений

### 4. Улучшен UI кнопки
- Правильная работа с `.btn-text` и `.btn-icon`
- Визуальная обратная связь при отправке
- Автоматическое восстановление кнопки

## Как проверить работу:

### Шаг 1: Запустите backend сервер

```bash
cd server
npm start
```

Вы должны увидеть:
```
🚀 Сервер запущен!
📡 Порт: 3001
🌐 Health check: http://localhost:3001/health
📧 API endpoint: http://localhost:3001/api/contact
```

### Шаг 2: Откройте сайт в браузере

Откройте `index.html` в браузере или через локальный сервер.

### Шаг 3: Откройте консоль браузера

Нажмите F12 → вкладка Console

### Шаг 4: Заполните и отправьте форму

Вы должны увидеть в консоли:
- `🔗 API Base URL: http://localhost:3001`
- `📤 Отправка запроса на: http://localhost:3001/api/contact`
- `📝 Тело запроса: {...}`
- `📥 Статус ответа: 200`
- `✅ Ответ сервера: {...}`

### Шаг 5: Проверьте логи сервера

В терминале, где запущен сервер, должны быть:
- `📧 Получен запрос на отправку сообщения`
- `📝 Полученные данные: {...}`
- `✅ Email успешно отправлен!`

## Возможные проблемы:

### Проблема: "Failed to fetch" или "NetworkError"
**Решение:** 
1. Убедитесь, что backend сервер запущен
2. Проверьте порт в `.env` (должен быть 3001)
3. Проверьте, что нет блокировки файрволом

### Проблема: CORS ошибка
**Решение:**
1. Проверьте `FRONTEND_URL` в `server/.env`
2. Убедитесь, что сайт открыт на том же URL, что указан в `FRONTEND_URL`

### Проблема: "Ошибка валидации данных"
**Решение:**
1. Проверьте, что все поля заполнены
2. Имя: минимум 2 символа
3. Email: корректный формат (например, test@example.com)
4. Сообщение: минимум 5 символов

### Проблема: Email не отправляется
**Решение:**
1. Проверьте настройки SMTP в `server/.env`
2. Убедитесь, что используете пароль приложения Gmail (не обычный пароль)
3. Проверьте логи сервера для деталей ошибки

## Тестирование через curl:

```bash
curl -X POST http://localhost:3001/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "message": "Test message from curl"
  }'
```

Если это работает, значит backend работает правильно, и проблема в frontend.

## Дополнительная отладка:

1. Откройте DevTools (F12)
2. Перейдите на вкладку Network
3. Отправьте форму
4. Найдите запрос к `/api/contact`
5. Проверьте:
   - Request URL
   - Request Method (должен быть POST)
   - Request Payload (данные формы)
   - Response Status (должен быть 200)
   - Response Body (ответ сервера)
