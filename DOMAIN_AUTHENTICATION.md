# 🔐 Настройка Domain Authentication в SendGrid

## Зачем это нужно

Domain Authentication (SPF/DKIM) в SendGrid необходима для:
- ✅ Улучшения доставляемости писем (меньше вероятность попасть в спам)
- ✅ Повышения репутации отправителя
- ✅ Автоматической верификации всех email адресов на вашем домене

**Без Domain Authentication письма могут попадать в спам!**

---

## 📋 Что нужно сделать

### Шаг 1: Получите домен

У вас должен быть свой домен (например, `example.com`).

Если домена нет:
- Купите домен на Namecheap, GoDaddy, Cloudflare и т.д.
- Или используйте поддомен (например, `mail.yourdomain.com`)

### Шаг 2: Настройте Domain Authentication в SendGrid

1. **Войдите в SendGrid Dashboard**: https://app.sendgrid.com

2. **Перейдите в настройки аутентификации**:
   - Settings → **Sender Authentication** → **Domain Authentication**
   - Или прямая ссылка: https://app.sendgrid.com/settings/sender_auth/domains

3. **Нажмите "Authenticate Your Domain"**

4. **Выберите DNS провайдера**:
   - Если ваш домен на Namecheap, GoDaddy, Cloudflare и т.д. - выберите из списка
   - Если нет в списке - выберите "Other"

5. **Введите домен**:
   - Введите ваш домен (например, `example.com`)
   - Или поддомен (например, `mail.example.com`)

6. **Скопируйте DNS записи**:
   - SendGrid покажет список DNS записей, которые нужно добавить
   - Обычно это:
     - CNAME записи для SPF
     - CNAME записи для DKIM
     - TXT записи

### Шаг 3: Добавьте DNS записи в ваш DNS провайдер

1. **Войдите в панель управления вашего DNS провайдера**:
   - Namecheap: https://www.namecheap.com/myaccount/login/
   - GoDaddy: https://www.godaddy.com/
   - Cloudflare: https://dash.cloudflare.com/
   - Или где у вас настроен DNS

2. **Найдите раздел DNS / DNS Records**

3. **Добавьте все записи, которые показал SendGrid**:
   - Тип записи: CNAME или TXT
   - Имя хоста: как указано в SendGrid
   - Значение: как указано в SendGrid
   - TTL: можно оставить по умолчанию

4. **Сохраните изменения**

### Шаг 4: Подождите верификацию

1. **Вернитесь в SendGrid Dashboard**

2. **Нажмите "Verify"** или дождитесь автоматической проверки

3. **Проверка может занять от нескольких минут до 48 часов** (обычно 15-30 минут)

4. **Статус должен измениться на "Verified"** (зелёная галочка)

---

## 📧 Обновление EMAIL_FROM

После верификации домена:

1. **Обновите переменную `EMAIL_FROM` на Render**:
   - Используйте адрес вида: `contact@ваш-домен.com`
   - Например: `contact@example.com`
   - Или: `noreply@example.com`
   - Или: `info@example.com`

2. **Все email адреса на верифицированном домене автоматически verified**:
   - Не нужно отдельно верифицировать каждый адрес
   - Можно использовать любой адрес на домене: `contact@`, `info@`, `noreply@` и т.д.

3. **Обновите переменную в Render Dashboard**:
   - Render Dashboard → ваш сервис → Environment
   - Измените `EMAIL_FROM` на `contact@ваш-домен.com`
   - Сделайте Manual Deploy

---

## ✅ Проверка

### 1. Проверьте статус в SendGrid

SendGrid Dashboard → Settings → Sender Authentication → Domain Authentication

Статус должен быть **"Verified"** (зелёная галочка).

### 2. Отправьте тестовое письмо

1. Заполните форму на сайте
2. Проверьте логи на Render - должно быть "✅ Email успешно отправлен"
3. Проверьте почту - письмо должно прийти (не в спам!)

### 3. Проверьте заголовки письма

В полученном письме проверьте заголовки:
- Должен быть SPF: `pass`
- Должен быть DKIM: `pass`
- DMARC: `pass`

---

## ❌ Troubleshooting

### Проблема: DNS записи не проходят проверку

**Решение:**
1. Убедитесь, что все DNS записи добавлены правильно
2. Проверьте, что нет опечаток в именах и значениях
3. Подождите 15-30 минут (DNS может распространяться медленно)
4. Используйте онлайн-инструменты для проверки DNS:
   - https://mxtoolbox.com/
   - https://www.whatsmydns.net/

### Проблема: Домен не верифицируется

**Решение:**
1. Проверьте, что все DNS записи добавлены
2. Убедитесь, что TTL не слишком большой (рекомендуется 3600 или меньше)
3. Попробуйте очистить DNS кеш
4. Обратитесь в поддержку SendGrid

### Проблема: Письма всё ещё в спаме

**Решение:**
1. Убедитесь, что Domain Authentication настроена и verified
2. Проверьте, что `EMAIL_FROM` на верифицированном домене
3. Подождите несколько дней (репутация отправителя улучшается со временем)
4. Отметьте письма как "Не спам" в почтовом клиенте

---

## 📝 Пример конфигурации

### До (без Domain Authentication):
```env
EMAIL_FROM=your-email@gmail.com
```
❌ Письма могут попадать в спам

### После (с Domain Authentication):
```env
EMAIL_FROM=contact@example.com
```
✅ Письма доставляются нормально

---

## 🔗 Полезные ссылки

- **SendGrid Domain Authentication**: https://app.sendgrid.com/settings/sender_auth/domains
- **SendGrid Documentation**: https://docs.sendgrid.com/ui/account-and-settings/how-to-set-up-domain-authentication
- **DNS Checker**: https://mxtoolbox.com/
- **SPF Record Checker**: https://mxtoolbox.com/spf.aspx

---

## ✅ Чек-лист

- [ ] Есть свой домен
- [ ] Настроена Domain Authentication в SendGrid
- [ ] Все DNS записи добавлены в DNS провайдера
- [ ] Домен verified в SendGrid (статус "Verified")
- [ ] `EMAIL_FROM` обновлён на `contact@ваш-домен.com` в Render
- [ ] Сделан Manual Deploy на Render
- [ ] Протестирована отправка письма
- [ ] Письмо пришло (не в спам!)

После выполнения всех шагов письма должны доставляться нормально, без попадания в спам.
