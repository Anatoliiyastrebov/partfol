# SendGrid API Configuration
# Получите API ключ в SendGrid Dashboard: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here

# Email Configuration
# ВАЖНО: Для нормальной доставляемости (чтобы не попадали в спам) нужна Domain Authentication в SendGrid.
# EMAIL_FROM должен быть на домене, который аутентифицирован в SendGrid (SPF/DKIM).
# Рекомендуется использовать адрес вида: contact@ваш-домен.com
# 
# Настройка Domain Authentication:
# 1. SendGrid Dashboard → Settings → Sender Authentication → Domain Authentication
# 2. Добавьте ваш домен и следуйте инструкциям по настройке DNS записей
# 3. После верификации домена, все email адреса на этом домене автоматически verified
EMAIL_FROM=contact@your-domain.com

# Email получателя (куда будут приходить письма с формы)
EMAIL_TO=your-email@example.com

# Server Configuration
PORT=3000

# Frontend URL (для CORS, опционально)
FRONTEND_URL=http://localhost:8000
