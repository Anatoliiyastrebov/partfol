# Backend для формы обратной связи

Backend сервер на Node.js + Express для обработки формы обратной связи и отправки email через SMTP.

## 📋 Требования

- Node.js 18 или выше
- npm или yarn
- Email аккаунт с поддержкой SMTP (Gmail, Yandex, Mail.ru и т.д.)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd server
npm install
```

### 2. Настройка переменных окружения

```bash
# Скопируйте пример файла
cp .env.example .env

# Откройте .env и заполните своими данными
nano .env  # или используйте любой текстовый редактор
```

### 3. Заполнение .env файла

Откройте файл `.env` и заполните следующие переменные:

#### Для Gmail:

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your.email@gmail.com
SMTP_PASS=ваш_пароль_приложения
EMAIL_TO=your.email@gmail.com
PORT=3000
FRONTEND_URL=http://localhost:8000
```

**Как получить пароль приложения для Gmail:**

1. Включите двухфакторную аутентификацию в настройках Google аккаунта
2. Перейдите на: https://myaccount.google.com/apppasswords
3. Выберите "Почта" и "Другое устройство"
4. Введите название (например: "Portfolio Backend")
5. Скопируйте сгенерированный 16-символьный пароль
6. Вставьте его в `SMTP_PASS` в файле `.env`

#### Для Yandex:

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_USER=your.email@yandex.ru
SMTP_PASS=ваш_пароль_приложения
EMAIL_TO=your.email@yandex.ru
PORT=3000
FRONTEND_URL=http://localhost:8000
```

**Как получить пароль приложения для Yandex:**

1. Включите двухфакторную аутентификацию
2. Перейдите в настройки безопасности
3. Создайте пароль приложения
4. Используйте его в `SMTP_PASS`

#### Для Mail.ru:

```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=587
SMTP_USER=your.email@mail.ru
SMTP_PASS=ваш_пароль_приложения
EMAIL_TO=your.email@mail.ru
PORT=3000
FRONTEND_URL=http://localhost:8000
```

### 4. Запуск сервера

```bash
npm start
```

Или для автоматической перезагрузки при изменениях:

```bash
npm run dev
```

Вы должны увидеть:

```
🚀 Сервер запущен!
📡 Порт: 3000
🌐 Health check: http://localhost:3000/health
📧 API endpoint: http://localhost:3000/api/contact
🔗 Frontend URL: http://localhost:8000
✅ Подключение к SMTP серверу успешно установлено
```

## 🧪 Тестирование

### Проверка здоровья сервера

```bash
curl http://localhost:3000/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T12:00:00.000Z",
  "uptime": 123.456
}
```

### Тестовая отправка сообщения

```bash
curl -X POST http://localhost:3000/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовое Имя",
    "email": "test@example.com",
    "message": "Это тестовое сообщение для проверки работы формы обратной связи."
  }'
```

Ожидаемый ответ при успехе:
```json
{
  "success": true,
  "message": "Сообщение успешно отправлено",
  "messageId": "..."
}
```

Ожидаемый ответ при ошибке валидации:
```json
{
  "success": false,
  "message": "Ошибка валидации данных",
  "errors": ["Поле 'name' обязательно и должно быть строкой"]
}
```

## 📁 Структура проекта

```
server/
├── server.js          # Основной файл сервера
├── package.json        # Зависимости проекта
├── .env.example        # Пример конфигурации
├── .env               # Ваша конфигурация (не коммитится)
├── .gitignore         # Игнорируемые файлы
└── README.md          # Эта инструкция
```

## 🔧 API Endpoints

### POST /api/contact

Отправка сообщения через форму обратной связи.

**Тело запроса:**
```json
{
  "name": "Имя отправителя",
  "email": "email@example.com",
  "message": "Текст сообщения"
}
```

**Успешный ответ (200):**
```json
{
  "success": true,
  "message": "Сообщение успешно отправлено",
  "messageId": "message-id-from-smtp"
}
```

**Ошибка валидации (400):**
```json
{
  "success": false,
  "message": "Ошибка валидации данных",
  "errors": ["Список ошибок валидации"]
}
```

**Ошибка сервера (500):**
```json
{
  "success": false,
  "message": "Произошла ошибка при отправке сообщения. Попробуйте позже."
}
```

### GET /health

Проверка работоспособности сервера.

**Ответ (200):**
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T12:00:00.000Z",
  "uptime": 123.456
}
```

## ⚠️ Частые ошибки и их решение

### 1. "Ошибка подключения к SMTP серверу"

**Причины:**
- Неправильный `SMTP_HOST` или `SMTP_PORT`
- Неверные `SMTP_USER` или `SMTP_PASS`
- Использование обычного пароля вместо пароля приложения

**Решение:**
- Проверьте правильность хоста и порта для вашего почтового провайдера
- Убедитесь, что используете пароль приложения, а не обычный пароль
- Для Gmail обязательно включите двухфакторную аутентификацию

### 2. "Invalid login" или "Authentication failed"

**Причина:** Неправильный пароль приложения или обычный пароль

**Решение:**
- Создайте новый пароль приложения в настройках вашего почтового аккаунта
- Убедитесь, что скопировали пароль полностью (обычно 16 символов для Gmail)

### 3. "Connection timeout"

**Причина:** Проблемы с сетью или неправильный порт

**Решение:**
- Проверьте интернет-соединение
- Попробуйте другой порт (587 для TLS или 465 для SSL)
- Проверьте, не блокирует ли файрвол подключения

### 4. "CORS error" в браузере

**Причина:** Неправильный `FRONTEND_URL` в `.env`

**Решение:**
- Убедитесь, что `FRONTEND_URL` в `.env` совпадает с URL, на котором открыт фронтенд
- Если фронтенд на `http://localhost:8000`, то `FRONTEND_URL=http://localhost:8000`

### 5. "Cannot find module" при запуске

**Причина:** Зависимости не установлены

**Решение:**
```bash
cd server
npm install
```

### 6. Письма не приходят

**Проверьте:**
- Папку "Спам" в почтовом ящике
- Правильность `EMAIL_TO` в `.env`
- Логи сервера на наличие ошибок
- Что сервер успешно подключился к SMTP (должно быть сообщение при запуске)

## 🔒 Безопасность

- **Никогда не коммитьте файл `.env` в git!**
- Используйте пароли приложений, а не обычные пароли
- В продакшене используйте переменные окружения хостинга вместо `.env`
- Ограничьте `FRONTEND_URL` только вашим доменом в продакшене

## 📝 Логирование

Сервер выводит подробные логи в консоль:
- `📧` - получение запроса
- `✅` - успешная операция
- `❌` - ошибка
- `📤` - отправка email
- `📝` - данные формы

## 🚀 Деплой в продакшен

Для деплоя на хостинг (Heroku, Railway, Render и т.д.):

1. Установите переменные окружения на хостинге
2. Убедитесь, что `FRONTEND_URL` указывает на ваш домен
3. Запустите сервер командой `npm start`

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи сервера
2. Убедитесь, что все переменные в `.env` заполнены правильно
3. Проверьте раздел "Частые ошибки" выше
