# 🔧 Исправление ошибки 404 на Render

## ✅ Что исправлено:

Добавлен корневой роут `/` в `server/server.js`, который возвращает информацию о сервере вместо 404.

## 📋 Проверка настроек Render:

### 1. Проверьте настройки сервиса в Render Dashboard:

**Важные параметры:**
- **Root Directory:** `server` ⚠️ (обязательно!)
- **Build Command:** `npm install`
- **Start Command:** `npm start`
- **Environment:** `Node`

### 2. Проверьте, что сервис запущен:

1. Зайдите в Render Dashboard
2. Выберите ваш сервис `portfolio-backend`
3. Проверьте статус:
   - ✅ **Live** - сервис работает
   - ⏸️ **Suspended** - сервис "заснул" (Free план)
   - ❌ **Failed** - ошибка запуска

### 3. Если сервис "заснул" (Free план):

На Free плане Render останавливает сервисы после 15 минут неактивности.

**Решение:**
- Первый запрос после "сна" может занять 30-60 секунд (холодный старт)
- Просто подождите и обновите страницу
- Или используйте сервисы типа UptimeRobot для периодических запросов к `/health`

### 4. Проверьте логи:

Render Dashboard → ваш сервис → **Logs**

Должны быть логи:
```
🚀 Сервер запущен!
📡 Порт: 10000
🌐 Health check: http://localhost:10000/health
📧 API endpoint: http://localhost:10000/api/contact
```

### 5. Проверьте переменные окружения:

Убедитесь, что все переменные добавлены:
- `SMTP_HOST`
- `SMTP_PORT`
- `SMTP_USER`
- `SMTP_PASS`
- `EMAIL_TO`
- `PORT` (опционально, Render установит автоматически)

## 🧪 Тестирование:

### Проверка корневого роута:
```
https://ваш-сервис.onrender.com/
```

Должен вернуться JSON:
```json
{
  "status": "ok",
  "message": "Portfolio Backend API",
  "version": "1.0.0",
  "endpoints": {
    "health": "/health",
    "contact": "POST /api/contact"
  },
  "timestamp": "...",
  "uptime": ...
}
```

### Проверка health endpoint:
```
https://ваш-сервис.onrender.com/health
```

Должен вернуться:
```json
{
  "status": "ok",
  "timestamp": "...",
  "uptime": ...
}
```

### Проверка API endpoint:
```
POST https://ваш-сервис.onrender.com/api/contact
```

## ❌ Если всё ещё 404:

1. **Проверьте URL:**
   - Убедитесь, что используете правильный URL Render
   - Формат: `https://portfolio-backend-xxxxx.onrender.com`

2. **Сделайте Manual Deploy:**
   - Render Dashboard → Deployments → ⋯ → Manual Deploy

3. **Проверьте Root Directory:**
   - Должно быть: `server` (не корень проекта!)

4. **Проверьте логи на ошибки:**
   - Render Dashboard → Logs
   - Ищите ошибки запуска

5. **Проверьте переменные окружения:**
   - Если переменные не заданы, сервер может не запуститься

## 🔄 После исправления:

1. Render автоматически передеплоит после push (уже отправлено)
2. Или сделайте Manual Deploy вручную
3. Подождите 2-5 минут
4. Проверьте URL снова

---

## 📝 Изменения в коде:

Добавлен корневой роут в `server/server.js`:

```javascript
app.get('/', (req, res) => {
    res.json({
        status: 'ok',
        message: 'Portfolio Backend API',
        version: '1.0.0',
        endpoints: {
            health: '/health',
            contact: 'POST /api/contact'
        },
        timestamp: new Date().toISOString(),
        uptime: process.uptime()
    });
});
```

Теперь при открытии корневого URL будет возвращаться информация о сервере вместо 404.
