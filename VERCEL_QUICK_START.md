# 🚀 Быстрый старт: Форма на Vercel

## Проблема решена! ✅

Создана Vercel Serverless Function для обработки формы. Теперь нужно только настроить переменные окружения.

## ⚡ Что нужно сделать СЕЙЧАС:

### 1. Добавьте переменные окружения в Vercel

Зайдите на https://vercel.com/dashboard → Ваш проект → **Settings** → **Environment Variables**

Добавьте 5 переменных:

| Переменная | Значение | Пример |
|-----------|----------|--------|
| `SMTP_HOST` | `smtp.gmail.com` | smtp.gmail.com |
| `SMTP_PORT` | `587` | 587 |
| `SMTP_USER` | Ваш Gmail | your-email@gmail.com |
| `SMTP_PASS` | Пароль приложения Gmail | xxxx xxxx xxxx xxxx |
| `EMAIL_TO` | Куда приходят письма | your-email@gmail.com |

**ВАЖНО:** Отметьте все окружения (Production, Preview, Development) для каждой переменной!

### 2. Получите пароль приложения Gmail

1. Включите 2FA: https://myaccount.google.com/security
2. Создайте пароль: https://myaccount.google.com/apppasswords
3. Выберите "Почта" → "Другое устройство" → "Vercel"
4. Скопируйте 16-значный пароль (без пробелов)

### 3. Передеплойте проект

После добавления переменных:

**Вариант А (через веб-интерфейс):**
- Deployments → три точки (⋯) → **Redeploy**

**Вариант Б (через Git):**
```bash
git commit --allow-empty -m "Redeploy for env vars"
git push
```

### 4. Проверьте работу

1. Откройте ваш сайт на Vercel
2. Откройте консоль (F12)
3. Заполните форму
4. Должно быть: `✅ Сообщение успешно отправлено!`

## 📋 Что было создано:

- ✅ `api/contact.js` - Serverless function для Vercel
- ✅ `vercel.json` - Конфигурация Vercel
- ✅ `package.json` - Зависимости (nodemailer)
- ✅ Обновлен `script.js` - автоматически определяет Vercel

## 🔍 Проверка логов:

Vercel Dashboard → **Functions** → `contact` → **Logs**

Там вы увидите все логи отправки писем.

## ❌ Если не работает:

1. **Проверьте переменные** - все 5 должны быть добавлены
2. **Проверьте пароль** - используйте пароль приложения, не обычный
3. **Сделайте редеплой** - после добавления переменных обязателен
4. **Проверьте логи** - в Vercel Dashboard → Functions

## 📚 Подробная инструкция:

См. `VERCEL_ENV_SETUP.md` для детальной настройки.
