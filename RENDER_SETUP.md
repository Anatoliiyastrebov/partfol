# 🚀 Деплой Backend на Render.com - Пошаговая инструкция

## 📋 Что нужно сделать

Настроить Express backend на Render.com для обработки формы обратной связи.

---

## ✅ ШАГ 1: Подготовка GitHub репозитория

### 1.1. Убедитесь, что код загружен на GitHub

```bash
git status
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```

### 1.2. Проверьте структуру проекта

Убедитесь, что папка `server/` содержит:
- `server.js` - основной файл сервера
- `package.json` - зависимости
- `.env.example` - пример переменных окружения

---

## 🔧 ШАГ 2: Создание Web Service на Render

### 2.1. Регистрация/Вход на Render

1. Перейдите на https://render.com
2. Войдите через GitHub (рекомендуется) или создайте аккаунт

### 2.2. Создание нового Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Выберите ваш репозиторий `Anatoliiyastrebov/partfol`
3. Нажмите **"Connect"**

### 2.3. Настройка Web Service

Заполните следующие поля:

#### Основные настройки:

- **Name:** `portfolio-backend` (или любое другое имя)
- **Region:** Выберите ближайший регион (например, `Frankfurt (EU)` или `Oregon (US West)`)
- **Branch:** `main` (или ваша основная ветка)
- **Root Directory:** `server` ⚠️ **ВАЖНО!**
- **Runtime:** `Node`
- **Build Command:** `npm install`
- **Start Command:** `npm start`

#### Environment Variables (переменные окружения):

Нажмите **"Add Environment Variable"** и добавьте каждую:

1. **SMTP_HOST**
   - Key: `SMTP_HOST`
   - Value: `smtp.gmail.com`

2. **SMTP_PORT**
   - Key: `SMTP_PORT`
   - Value: `587`

3. **SMTP_USER**
   - Key: `SMTP_USER`
   - Value: `ваш_email@gmail.com` (например, `anatoliiyastrebov@gmail.com`)

4. **SMTP_PASS**
   - Key: `SMTP_PASS`
   - Value: `ваш_пароль_приложения_gmail` (16 символов без пробелов)
   - ⚠️ **ВАЖНО:** Используйте пароль приложения Gmail, не обычный пароль!

5. **EMAIL_TO**
   - Key: `EMAIL_TO`
   - Value: `ваш_email@gmail.com` (куда приходят письма)

6. **FRONTEND_URL** (опционально)
   - Key: `FRONTEND_URL`
   - Value: `https://ваш-сайт.vercel.app` (URL вашего фронтенда на Vercel)

7. **NODE_ENV**
   - Key: `NODE_ENV`
   - Value: `production`

8. **PORT** (опционально, Render автоматически устанавливает)
   - Key: `PORT`
   - Value: `10000` (или оставьте пустым, Render установит автоматически)

### 2.4. План и настройки

- **Instance Type:** `Free` (для начала, можно обновить позже)
- **Auto-Deploy:** `Yes` (автоматический деплой при push в GitHub)

### 2.5. Создание сервиса

Нажмите **"Create Web Service"**

---

## 📧 ШАГ 3: Получение пароля приложения Gmail

Если у вас еще нет пароля приложения Gmail:

### 3.1. Включите двухфакторную аутентификацию

1. Перейдите: https://myaccount.google.com/security
2. Включите **"Двухэтапная аутентификация"**

### 3.2. Создайте пароль приложения

1. Перейдите: https://myaccount.google.com/apppasswords
2. Выберите **"Почта"**
3. Выберите **"Другое устройство"**
4. Введите название: **"Render Portfolio Backend"**
5. Нажмите **"Создать"**
6. **Скопируйте 16-значный пароль** (например: `abcd efgh ijkl mnop`)
7. **Используйте БЕЗ пробелов** в Render: `abcdefghijklmnop`

---

## ⏳ ШАГ 4: Ожидание деплоя

После создания сервиса Render начнет деплой:

1. Вы увидите процесс сборки в логах
2. Дождитесь завершения (обычно 2-5 минут)
3. После успешного деплоя вы получите URL вида:
   ```
   https://portfolio-backend.onrender.com
   ```

### Проверка работы:

Откройте в браузере:
```
https://ваш-сервис.onrender.com/health
```

Должен вернуться JSON:
```json
{
  "status": "ok",
  "timestamp": "...",
  "uptime": ...
}
```

---

## 🔗 ШАГ 5: Обновление Frontend (script.js)

### 5.1. Получите URL вашего Render сервиса

После деплоя скопируйте URL (например: `https://portfolio-backend.onrender.com`)

### 5.2. Обновите script.js

Откройте `script.js` и найдите строку:

```javascript
return process.env.RENDER_API_URL || 'https://portfolio-backend.onrender.com';
```

Замените `'https://portfolio-backend.onrender.com'` на ваш реальный URL Render.

**Или** создайте переменную окружения на Vercel:
- Key: `RENDER_API_URL`
- Value: `https://ваш-сервис.onrender.com`

### 5.3. Закоммитьте и запушьте изменения

```bash
git add script.js
git commit -m "Update API URL for Render backend"
git push origin main
```

Vercel автоматически передеплоит фронтенд.

---

## ✅ ШАГ 6: Проверка работы

### 6.1. Откройте ваш сайт на Vercel

### 6.2. Откройте консоль браузера (F12 → Console)

### 6.3. Заполните и отправьте форму

### 6.4. Проверьте логи:

**В консоли браузера должно быть:**
```
🔗 API Base URL: https://portfolio-backend.onrender.com
📤 Отправка запроса на: https://portfolio-backend.onrender.com/api/contact
📝 Тело запроса: {...}
📥 Статус ответа: 200
✅ Сообщение успешно отправлено!
```

**В Render Dashboard → Logs должно быть:**
```
📧 Получен запрос на отправку сообщения
📝 Полученные данные: {...}
📤 Отправка email...
✅ Email успешно отправлен!
```

---

## 🔍 Проверка логов в Render

1. Зайдите в Render Dashboard
2. Выберите ваш сервис `portfolio-backend`
3. Перейдите в **"Logs"**
4. Там будут все логи выполнения

---

## ❌ Troubleshooting

### Проблема: "Service failed to start"

**Причина:** Неправильные переменные окружения или ошибка в коде

**Решение:**
1. Проверьте логи в Render Dashboard → Logs
2. Убедитесь, что все переменные окружения добавлены
3. Проверьте, что `Root Directory` установлен в `server`

### Проблема: "Ошибка подключения к SMTP серверу"

**Причина:** Неправильные SMTP настройки или пароль

**Решение:**
1. Проверьте правильность `SMTP_HOST` (должно быть `smtp.gmail.com`)
2. Проверьте правильность `SMTP_PORT` (должно быть `587`)
3. Убедитесь, что используете **пароль приложения**, а не обычный пароль
4. Проверьте, что в пароле нет пробелов

### Проблема: "Failed to fetch" или CORS ошибка

**Причина:** CORS не настроен правильно

**Решение:**
1. Проверьте, что в `server.js` CORS разрешает запросы с вашего Vercel домена
2. Убедитесь, что `FRONTEND_URL` в Render указывает на ваш Vercel сайт
3. Проверьте логи в Render Dashboard

### Проблема: Сервис "засыпает" на Free плане

**Причина:** Render Free план останавливает сервисы после 15 минут неактивности

**Решение:**
1. Первый запрос после "сна" может занять 30-60 секунд (холодный старт)
2. Рассмотрите переход на платный план для постоянной работы
3. Или используйте сервисы типа UptimeRobot для периодических запросов к `/health`

### Проблема: Переменные окружения не работают

**Причина:** Переменные не применены или сервис не передеплоен

**Решение:**
1. Убедитесь, что переменные добавлены в Render Dashboard
2. Сделайте **Manual Deploy** после добавления переменных
3. Проверьте, что переменные добавлены для правильного окружения

---

## 📝 Структура проекта для Render

```
portfol/
├── server/                    ← Backend (деплоится на Render)
│   ├── server.js              ← Express сервер
│   ├── package.json           ← Зависимости
│   ├── .env.example           ← Пример переменных
│   └── README.md
├── api/                       ← Vercel serverless (можно удалить)
│   └── contact.js
├── index.html                 ← Frontend (деплоится на Vercel)
├── styles.css
├── script.js                  ← Обновлен для Render API
├── package.json
├── vercel.json
└── render.yaml                ← Конфигурация Render (опционально)
```

---

## 🎯 Быстрая проверка

После настройки откройте консоль браузера (F12) и отправьте форму. Должно быть:

✅ `🔗 API Base URL: https://ваш-сервис.onrender.com`
✅ `📤 Отправка запроса на: https://ваш-сервис.onrender.com/api/contact`
✅ `📥 Статус ответа: 200`
✅ `✅ Сообщение успешно отправлено!`

---

## 📊 Сравнение Render vs Vercel Serverless

| Параметр | Render (Web Service) | Vercel (Serverless) |
|----------|---------------------|---------------------|
| **Тип** | Постоянный процесс | Функции по запросу |
| **Холодный старт** | Да (Free план) | Да (всегда) |
| **Стоимость** | Free (с ограничениями) | Free (с ограничениями) |
| **Простота** | Проще для Express | Требует адаптации |
| **Логи** | Хорошие | Хорошие |
| **Масштабирование** | Автоматическое | Автоматическое |

---

## ✅ Чек-лист деплоя на Render:

- [ ] Код загружен на GitHub
- [ ] Создан Web Service на Render
- [ ] `Root Directory` установлен в `server`
- [ ] Все 5 переменных окружения добавлены
- [ ] Используется пароль приложения Gmail
- [ ] Сервис успешно задеплоен
- [ ] `/health` endpoint работает
- [ ] `script.js` обновлен с правильным Render URL
- [ ] Фронтенд передеплоен на Vercel
- [ ] Форма работает и отправляет письма

---

## 🎉 Готово!

После выполнения всех шагов ваш backend будет работать на Render, а frontend на Vercel. Форма обратной связи будет отправлять письма на ваш Gmail!
