# 🔧 Настройка статического сайта на Vercel с кастомным доменом

## 📋 Важные нюансы для статического сайта

Ваш проект - это **статический HTML/CSS/JS сайт** (не React/Next.js), поэтому некоторые настройки отличаются.

---

## ✅ Настройка для статического сайта

### 1. Проверка структуры проекта

Ваш проект должен иметь такую структуру:
```
portfol/
├── index.html          ← Главная страница
├── styles.css
├── script.js
├── images/
└── ...
```

**Убедитесь, что `index.html` находится в корне проекта.**

---

### 2. Настройка Vercel для статического сайта

Vercel автоматически определяет статический сайт, но можно создать `vercel.json` для явной конфигурации:

#### Создайте `vercel.json` (если его нет):

```json
{
  "version": 2,
  "buildCommand": null,
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

**Или минимальная версия (если не нужен rewrite):**

```json
{
  "version": 2
}
```

---

### 3. Проверка DNS записей

#### Для домена на Spaceship:

1. **Получите DNS записи от Vercel:**
   - Vercel Dashboard → проект → Settings → Domains
   - Найдите домен `anatolii-yastrebov.top`
   - Vercel покажет, какие DNS записи нужно добавить

2. **Добавьте DNS записи в Spaceship:**
   - Войдите в Spaceship: https://www.spaceship.com/
   - My Domains → `anatolii-yastrebov.top` → DNS Management
   - Добавьте записи, которые показал Vercel

**Обычно это:**
- **A запись:** `@` → `76.76.21.21`
- **Или CNAME:** `@` → `cname.vercel-dns.com`
- **CNAME для www:** `www` → `cname.vercel-dns.com`

3. **Подождите 15-30 минут** для распространения DNS

---

### 4. SSL / HTTPS сертификат

✅ **Vercel автоматически выдаёт SSL сертификат** для кастомного домена.

**Процесс:**
1. После добавления DNS записей Vercel автоматически начнёт проверку
2. Когда DNS записи подтвердятся, Vercel выдаст SSL сертификат
3. Обычно это занимает **5-15 минут** после подтверждения DNS
4. Сайт будет доступен по HTTPS: `https://anatolii-yastrebov.top`

**Проверка SSL:**
- Статус домена в Vercel должен быть **"Valid"** или **"Configured"**
- В браузере должен быть зелёный замочек 🔒

---

### 5. Проверка деплоя

1. **Vercel Dashboard** → проект → **Deployments**
2. Убедитесь, что есть **успешные деплои**
3. Если деплоев нет - сделайте:
   - **Manual Deploy:** Deployments → ⋯ → Redeploy
   - Или **push в GitHub:** `git push origin main`

---

## 🔍 Диагностика пустой страницы

### Проверка 1: Консоль браузера

1. Откройте `https://anatolii-yastrebov.top`
2. **F12** → **Console**
3. Проверьте ошибки

**Возможные ошибки:**
- `Failed to load resource` - файлы не найдены
- `404 Not Found` - неправильный путь к файлам
- CORS ошибки - проблема с настройками

### Проверка 2: Network tab

1. **F12** → **Network**
2. Обновите страницу (F5)
3. Проверьте, какие файлы загружаются

**Должны загружаться:**
- ✅ `index.html` (статус 200)
- ✅ `styles.css` (статус 200)
- ✅ `script.js` (статус 200)
- ✅ `images/photo.jpg` (если есть)

**Если файлы не загружаются (404):**
- Проверьте структуру проекта
- Убедитесь, что файлы в корне проекта
- Проверьте деплой в Vercel

### Проверка 3: Исходный код страницы

1. Откройте `https://anatolii-yastrebov.top`
2. **Правый клик** → **View Page Source** (или Ctrl+U)
3. Проверьте, есть ли содержимое HTML

**Если HTML пустой:**
- Проблема с деплоем
- Проверьте логи деплоя в Vercel

---

## 📋 Чек-лист настройки

- [ ] Домен добавлен в Vercel (Settings → Domains)
- [ ] DNS записи добавлены в Spaceship (A или CNAME)
- [ ] Подождали 15-30 минут после добавления DNS
- [ ] Статус домена в Vercel: "Valid" или "Configured"
- [ ] SSL сертификат выдан (зелёный замочек в браузере)
- [ ] Проект задеплоен (есть успешные деплои)
- [ ] `index.html` находится в корне проекта
- [ ] Сайт открывается по HTTPS: `https://anatolii-yastrebov.top`
- [ ] Файлы загружаются (проверено через Network tab)
- [ ] Нет ошибок в консоли браузера

---

## ❌ Частые проблемы

### Проблема 1: DNS записи не добавлены

**Симптомы:** "Не удается найти DNS-адрес сайта"

**Решение:**
1. Добавьте DNS записи в Spaceship
2. Используйте точные значения, которые показал Vercel
3. Подождите 15-30 минут
4. Проверьте через https://www.whatsmydns.net/

### Проблема 2: SSL сертификат не выдан

**Симптомы:** Сайт не открывается по HTTPS

**Решение:**
1. Убедитесь, что DNS записи подтверждены
2. Подождите 5-15 минут после подтверждения DNS
3. Проверьте статус домена в Vercel
4. Если через час не работает - свяжитесь с поддержкой Vercel

### Проблема 3: Пустая страница

**Симптомы:** Сайт открывается, но пустой

**Решение:**
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте Network tab (какие файлы загружаются)
3. Проверьте, что проект задеплоен
4. Сделайте Manual Deploy в Vercel

### Проблема 4: Файлы не загружаются (404)

**Симптомы:** HTML загружается, но CSS/JS не загружаются

**Решение:**
1. Проверьте пути к файлам в `index.html`
2. Убедитесь, что файлы находятся в корне проекта
3. Проверьте структуру проекта в Vercel Dashboard

---

## 🔗 Полезные ссылки

- **Vercel Domains**: https://vercel.com/docs/concepts/projects/domains
- **Vercel Dashboard**: https://vercel.com/dashboard
- **Spaceship DNS**: https://www.spaceship.com/
- **DNS Checker**: https://www.whatsmydns.net/
- **SSL Checker**: https://www.ssllabs.com/ssltest/

---

## 📧 После настройки домена

Когда домен `anatolii-yastrebov.top` будет работать:

1. **Добавьте DNS записи для SendGrid** (как в `SENDGRID_DNS_RECORDS.md`)
2. **Настройте Domain Authentication** в SendGrid для `anatolii-yastrebov.top`
3. **Обновите EMAIL_FROM** на Render:
   ```
   contact@anatolii-yastrebov.top
   ```

После этого письма будут доставляться нормально, без попадания в спам!

---

## 💡 Важно для статического сайта

- ✅ **Не нужен `homepage` в package.json** (это для React)
- ✅ **Не нужен build** (просто деплой файлов)
- ✅ **Vercel автоматически определит** статический сайт
- ✅ **SSL выдаётся автоматически** после подтверждения DNS
- ✅ **Rewrite правила** обычно не нужны для простого статического сайта

Ваш проект - статический HTML/CSS/JS, поэтому большинство настроек применяются автоматически!
