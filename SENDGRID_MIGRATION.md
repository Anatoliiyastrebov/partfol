# 📧 Миграция с Nodemailer (SMTP) на SendGrid

## ✅ Что было изменено

### Изменённые файлы:

1. **`server/package.json`**
   - ❌ Удалён: `nodemailer`
   - ✅ Добавлен: `@sendgrid/mail`

2. **`server/server.js`**
   - Заменён импорт `nodemailer` на `@sendgrid/mail`
   - Переписана функция отправки email на SendGrid API
   - Обновлены переменные окружения

3. **`server/.env.example`**
   - Обновлён под SendGrid конфигурацию

---

## 📦 Команды для установки зависимостей

### В папке `server/`:

```bash
cd server

# Удаляем старую зависимость
npm uninstall nodemailer

# Устанавливаем новую зависимость
npm install @sendgrid/mail
```

Или одной командой:

```bash
cd server && npm uninstall nodemailer && npm install @sendgrid/mail
```

---

## 🔧 Настройка переменных окружения

### 1. Получите API ключ SendGrid

1. Зарегистрируйтесь/войдите на https://sendgrid.com
2. Перейдите в **Settings** → **API Keys**: https://app.sendgrid.com/settings/api_keys
3. Нажмите **"Create API Key"**
4. Название: `Portfolio Backend`
5. Права: **Full Access** (или **Mail Send**)
6. Скопируйте API ключ (начинается с `SG.`)

### 2. Verify Sender Email в SendGrid

**ВАЖНО:** Email отправителя должен быть verified в SendGrid!

1. Перейдите в **Settings** → **Sender Authentication**: https://app.sendgrid.com/settings/sender_auth/senders
2. Нажмите **"Create New Sender"**
3. Заполните форму:
   - **From Email:** ваш email (например, `anatoliiyastrebov@gmail.com`)
   - **From Name:** ваше имя
   - Подтвердите email (проверьте почту и перейдите по ссылке)

### 3. Создайте `.env` файл

В папке `server/` создайте файл `.env`:

```env
# SendGrid API Configuration
SENDGRID_API_KEY=SG.ваш_реальный_api_ключ_здесь

# Email Configuration
# EMAIL_FROM должен быть verified sender в SendGrid Dashboard
EMAIL_FROM=anatoliiyastrebov@gmail.com

# Email получателя (куда будут приходить письма с формы)
EMAIL_TO=anatoliiyastrebov@gmail.com

# Server Configuration
PORT=3000

# Frontend URL (для CORS, опционально)
FRONTEND_URL=http://localhost:8000
```

---

## 🚀 Настройка на Render

### 1. Обновите переменные окружения в Render Dashboard

Render Dashboard → ваш сервис → **Environment**

#### Удалите старые переменные:
- ❌ `SMTP_HOST`
- ❌ `SMTP_PORT`
- ❌ `SMTP_USER`
- ❌ `SMTP_PASS`

#### Добавьте новые переменные:

1. **SENDGRID_API_KEY**
   - Key: `SENDGRID_API_KEY`
   - Value: `SG.ваш_api_ключ` (полный ключ из SendGrid)

2. **EMAIL_FROM**
   - Key: `EMAIL_FROM`
   - Value: `ваш_email@gmail.com` (должен быть verified в SendGrid!)

3. **EMAIL_TO**
   - Key: `EMAIL_TO`
   - Value: `ваш_email@gmail.com` (куда приходят письма)

4. **PORT** (опционально)
   - Key: `PORT`
   - Value: `10000` (или оставьте пустым)

### 2. Переустановите зависимости

Render автоматически переустановит зависимости при следующем деплое, но можно сделать Manual Deploy:

1. Render Dashboard → Deployments → ⋯ → **Manual Deploy**

---

## ✅ Проверка работы

### 1. Локальная проверка

```bash
cd server
npm install
npm start
```

В консоли должно быть:
```
✅ SendGrid настроен
📧 Отправитель: ваш_email@gmail.com
📬 Получатель: ваш_email@gmail.com
⚠️  Убедитесь, что EMAIL_FROM является verified sender в SendGrid Dashboard
🚀 Сервер запущен!
```

### 2. Тест отправки через curl

```bash
curl -X POST http://localhost:3000/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "message": "Test message"
  }'
```

Должен вернуться:
```json
{
  "success": true,
  "message": "Сообщение успешно отправлено",
  "statusCode": 202
}
```

### 3. Проверка на Render

1. Откройте логи: Render Dashboard → Logs
2. При запуске должно быть:
   ```
   ✅ SendGrid настроен
   📧 Отправитель: ваш_email@gmail.com
   ```
3. Отправьте форму на сайте
4. В логах должно быть:
   ```
   📤 Отправка email через SendGrid...
   ✅ Email успешно отправлен через SendGrid!
   Status Code: 202
   ```

---

## ❌ Troubleshooting

### Ошибка: "The from address does not match a verified Sender Identity"

**Решение:**
1. Перейдите в SendGrid Dashboard → Settings → Sender Authentication
2. Убедитесь, что `EMAIL_FROM` добавлен как verified sender
3. Проверьте email и перейдите по ссылке подтверждения

### Ошибка: "Invalid API key"

**Решение:**
1. Проверьте, что `SENDGRID_API_KEY` начинается с `SG.`
2. Убедитесь, что скопировали полный ключ (обычно ~70 символов)
3. Проверьте, что ключ имеет права "Mail Send"

### Ошибка: "Connection timeout" на Render

**Решение:**
- SendGrid использует HTTP API, а не SMTP, поэтому timeout не должен возникать
- Если всё ещё есть проблемы, проверьте логи на Render

---

## 📊 Преимущества SendGrid

✅ **Надёжность:** HTTP API вместо SMTP (меньше проблем с подключением)  
✅ **Простота:** Не нужны пароли приложений Gmail  
✅ **Масштабируемость:** Лучше работает на облачных платформах  
✅ **Аналитика:** SendGrid предоставляет статистику отправки  
✅ **Бесплатный план:** 100 писем в день бесплатно  

---

## 🔄 Откат изменений (если нужно)

Если нужно вернуться к SMTP:

```bash
cd server
npm uninstall @sendgrid/mail
npm install nodemailer@^6.9.7
```

Затем верните старые переменные окружения и код из git истории.

---

## ✅ Чек-лист миграции

- [ ] Установлен `@sendgrid/mail` (`npm install @sendgrid/mail`)
- [ ] Удалён `nodemailer` (`npm uninstall nodemailer`)
- [ ] Создан API ключ в SendGrid
- [ ] Email отправителя verified в SendGrid
- [ ] Обновлены переменные окружения в `.env` (локально)
- [ ] Обновлены переменные окружения в Render Dashboard
- [ ] Удалены старые SMTP переменные из Render
- [ ] Сделан Manual Deploy на Render
- [ ] Проверены логи на Render (должно быть "✅ SendGrid настроен")
- [ ] Протестирована отправка формы

---

## 📝 Пример корректного .env (без реальных секретов)

```env
SENDGRID_API_KEY=SG.abcdefghijklmnopqrstuvwxyz1234567890ABCDEFGHIJKLMNOPQRSTUVWXYZ
EMAIL_FROM=your-email@gmail.com
EMAIL_TO=your-email@gmail.com
PORT=3000
FRONTEND_URL=http://localhost:8000
```

**⚠️ ВАЖНО:** Никогда не коммитьте `.env` файл в Git!
