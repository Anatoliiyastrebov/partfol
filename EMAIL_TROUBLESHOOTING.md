# 📧 Диагностика отправки email на Render

## ✅ Проверка настроек на Render

### 1. Проверьте переменные окружения в Render Dashboard

Зайдите в Render Dashboard → ваш сервис → **Environment**

Убедитесь, что все переменные добавлены и имеют правильные значения:

#### Обязательные переменные:

1. **SMTP_HOST**
   - Значение: `smtp.gmail.com`
   - ⚠️ Без пробелов в начале/конце

2. **SMTP_PORT**
   - Значение: `587`
   - ⚠️ Только цифры, без пробелов

3. **SMTP_USER**
   - Значение: `ваш_email@gmail.com` (например, `anatoliiyastrebov@gmail.com`)
   - ⚠️ Полный email адрес

4. **SMTP_PASS**
   - Значение: `пароль_приложения_gmail` (16 символов БЕЗ пробелов)
   - ⚠️ **ВАЖНО:** Используйте пароль приложения, НЕ обычный пароль Gmail!
   - Пример: `abcdefghijklmnop` (16 символов подряд)

5. **EMAIL_TO**
   - Значение: `ваш_email@gmail.com` (куда приходят письма)
   - ⚠️ Может быть тот же email, что и SMTP_USER

6. **PORT** (опционально)
   - Значение: `10000` или оставьте пустым (Render установит автоматически)

### 2. Проверьте логи на Render

Render Dashboard → ваш сервис → **Logs**

#### При запуске сервера должно быть:

```
✅ Подключение к SMTP серверу успешно установлено
🚀 Сервер запущен!
📡 Порт: 10000
```

#### Если видите ошибку:

```
❌ Ошибка подключения к SMTP серверу: ...
```

**Причины:**
- Неправильный `SMTP_HOST` или `SMTP_PORT`
- Неправильный `SMTP_USER` или `SMTP_PASS`
- Не используется пароль приложения Gmail

#### При отправке формы должно быть:

```
📧 Получен запрос на отправку сообщения
📝 Полученные данные: {...}
📤 Отправка email...
✅ Email успешно отправлен!
   Message ID: <...>
   Получатель: ваш_email@gmail.com
```

#### Если видите ошибку:

```
❌ Ошибка при отправке email: ...
```

**Причины:**
- Неправильные SMTP настройки
- Проблемы с паролем приложения
- Gmail блокирует подключение

---

## 🔑 Получение пароля приложения Gmail

### Шаг 1: Включите двухфакторную аутентификацию

1. Перейдите: https://myaccount.google.com/security
2. Найдите "Двухэтапная аутентификация"
3. Включите её (если еще не включена)

### Шаг 2: Создайте пароль приложения

1. Перейдите: https://myaccount.google.com/apppasswords
2. Выберите:
   - **Приложение:** Почта
   - **Устройство:** Другое (введите "Render Portfolio")
3. Нажмите **"Создать"**
4. **Скопируйте 16-значный пароль** (например: `abcd efgh ijkl mnop`)
5. **ВАЖНО:** Используйте БЕЗ пробелов в Render: `abcdefghijklmnop`

---

## 🧪 Тестирование

### 1. Проверьте health endpoint

Откройте в браузере:
```
https://portfolio-backend-db2d.onrender.com/health
```

Должен вернуться:
```json
{"status":"ok","timestamp":"...","uptime":...}
```

### 2. Проверьте отправку через curl

```bash
curl -X POST https://portfolio-backend-db2d.onrender.com/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "message": "Test message from curl"
  }'
```

Должен вернуться:
```json
{
  "success": true,
  "message": "Сообщение успешно отправлено",
  "messageId": "..."
}
```

### 3. Проверьте логи в Render

После отправки проверьте логи - должны быть сообщения об успешной отправке.

---

## ❌ Частые проблемы

### Проблема 1: "Ошибка подключения к SMTP серверу"

**Решение:**
1. Проверьте `SMTP_HOST` = `smtp.gmail.com` (точно так)
2. Проверьте `SMTP_PORT` = `587` (только цифры)
3. Убедитесь, что используете **пароль приложения**, а не обычный пароль
4. Проверьте, что в пароле нет пробелов

### Проблема 2: "Invalid login" или "Authentication failed"

**Решение:**
1. Убедитесь, что `SMTP_USER` - это полный email (например, `anatoliiyastrebov@gmail.com`)
2. Проверьте, что используете пароль приложения (16 символов)
3. Убедитесь, что в пароле нет пробелов
4. Попробуйте создать новый пароль приложения

### Проблема 3: Письма не приходят

**Решение:**
1. Проверьте папку **Спам** в Gmail
2. Проверьте логи на Render - должно быть "✅ Email успешно отправлен!"
3. Проверьте, что `EMAIL_TO` указан правильно
4. Убедитесь, что `EMAIL_TO` и `SMTP_USER` - это существующие email адреса

### Проблема 4: "Connection timeout"

**Решение:**
1. Проверьте интернет-соединение Render сервиса
2. Убедитесь, что `SMTP_PORT` = `587` (не 465)
3. Попробуйте перезапустить сервис на Render

---

## 📋 Чек-лист проверки

- [ ] Все 5 переменных окружения добавлены в Render
- [ ] `SMTP_HOST` = `smtp.gmail.com` (точно так, без пробелов)
- [ ] `SMTP_PORT` = `587` (только цифры)
- [ ] `SMTP_USER` = полный email адрес
- [ ] `SMTP_PASS` = пароль приложения (16 символов БЕЗ пробелов)
- [ ] `EMAIL_TO` = email, куда приходят письма
- [ ] Включена двухфакторная аутентификация Gmail
- [ ] Создан пароль приложения
- [ ] Сервис на Render запущен (статус "Live")
- [ ] В логах видно "✅ Подключение к SMTP серверу успешно установлено"
- [ ] После отправки формы в логах видно "✅ Email успешно отправлен!"

---

## 🔍 Проверка логов в реальном времени

1. Зайдите в Render Dashboard
2. Выберите ваш сервис
3. Перейдите в **Logs**
4. Отправьте форму на сайте
5. Смотрите логи в реальном времени

Должны увидеть:
```
📧 Получен запрос на отправку сообщения
📝 Полученные данные: {
  "name": "...",
  "email": "...",
  "message": "..."
}
📤 Отправка email...
✅ Email успешно отправлен!
   Message ID: <...>
   Получатель: ваш_email@gmail.com
```

---

## 📞 Если ничего не помогает

1. **Проверьте логи на Render** - там будет точная ошибка
2. **Создайте новый пароль приложения** Gmail
3. **Проверьте, что все переменные добавлены** для правильного окружения (Production)
4. **Сделайте Manual Deploy** после изменения переменных
5. **Проверьте папку Спам** в Gmail

---

## ✅ Правильная конфигурация

```
SMTP_HOST = smtp.gmail.com
SMTP_PORT = 587
SMTP_USER = anatoliiyastrebov@gmail.com
SMTP_PASS = abcdefghijklmnop (16 символов без пробелов)
EMAIL_TO = anatoliiyastrebov@gmail.com
```

После настройки письма должны приходить на email, указанный в `EMAIL_TO`.
