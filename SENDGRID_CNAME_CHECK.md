# 🔍 Проверка CNAME записей для SendGrid

## ❌ Проблема: Проверяете A запись вместо CNAME

Вы показываете IP адреса (`216.198.79.65`, `64.29.17.1` и т.д.), но для SendGrid нужны **CNAME записи**, которые должны указывать на **домены**, а не на IP адреса.

---

## ✅ Правильная проверка CNAME записей

### Проверка через What's My DNS:

1. Откройте: https://www.whatsmydns.net/

2. Проверьте **CNAME записи** (не A записи!):

**Запись 1:**
- Введите: `em5788.anatolii-yastrebov.top`
- **Выберите тип: CNAME** ⚠️ (не A!)
- **Должно показать:** `u58316196.wl197.sendgrid.net` (домен, не IP!)
- **Что показывает?**

**Запись 2:**
- Введите: `s1._domainkey.anatolii-yastrebov.top`
- **Выберите тип: CNAME** ⚠️
- **Должно показать:** `s1.domainkey.u58316196.wl197.sendgrid.net`
- **Что показывает?**

**Запись 3:**
- Введите: `s2._domainkey.anatolii-yastrebov.top`
- **Выберите тип: CNAME** ⚠️
- **Должно показать:** `s2.domainkey.u58316196.wl197.sendgrid.net`
- **Что показывает?**

**Запись 4:**
- Введите: `_dmarc.anatolii-yastrebov.top`
- **Выберите тип: TXT** ⚠️
- **Должно показать:** `v=DMARC1; p=none;`
- **Что показывает?**

---

## 🔧 Проверка записей в Spaceship

1. Spaceship → My Domains → `anatolii-yastrebov.top` → DNS Management

2. Проверьте **типы записей**:

**Должны быть CNAME записи:**
- ✅ CNAME: `em5788` → `u58316196.wl197.sendgrid.net`
- ✅ CNAME: `s1._domainkey` → `s1.domainkey.u58316196.wl197.sendgrid.net`
- ✅ CNAME: `s2._domainkey` → `s2.domainkey.u58316196.wl197.sendgrid.net`

**Должна быть TXT запись:**
- ✅ TXT: `_dmarc` → `v=DMARC1; p=none;`

**❌ НЕ должно быть A записей** для SendGrid (A записи только для Vercel: `@` → `76.76.21.21`)

---

## ⚠️ Если видите IP адреса вместо доменов

Это означает, что:
1. Либо проверяете **A запись** вместо **CNAME**
2. Либо в Spaceship добавлена **A запись** вместо **CNAME**

**Решение:**
1. В Spaceship проверьте тип записи для `em5788`
2. Должен быть тип **CNAME**, а не A
3. Если тип A - удалите и создайте заново как CNAME

---

## 📋 Правильная конфигурация в Spaceship

**Для Vercel (A запись):**
- Type: **A**
- Name: `@`
- Value: `76.76.21.21`
- TTL: 3600

**Для SendGrid (CNAME записи):**
- Type: **CNAME** ⚠️
- Name: `em5788`
- Value: `u58316196.wl197.sendgrid.net` (домен, не IP!)
- TTL: 3600

- Type: **CNAME** ⚠️
- Name: `s1._domainkey`
- Value: `s1.domainkey.u58316196.wl197.sendgrid.net` (домен, не IP!)
- TTL: 3600

- Type: **CNAME** ⚠️
- Name: `s2._domainkey`
- Value: `s2.domainkey.u58316196.wl197.sendgrid.net` (домен, не IP!)
- TTL: 3600

**Для SendGrid (TXT запись):**
- Type: **TXT** ⚠️
- Name: `_dmarc`
- Value: `v=DMARC1; p=none;` (английский текст!)
- TTL: 3600

---

## 🔍 Что проверить сейчас

1. **В Spaceship** - какой тип записи для `em5788`?
   - Должен быть **CNAME**, а не A

2. **What's My DNS** - проверьте тип **CNAME** (не A!):
   - https://www.whatsmydns.net/
   - Введите: `em5788.anatolii-yastrebov.top`
   - **Выберите: CNAME** (не A!)
   - Что показывает?

3. **Все ли записи правильного типа?**
   - 3 CNAME записи для SendGrid
   - 1 TXT запись для DMARC
   - 1 A запись для Vercel (это нормально)

---

## 💡 Важно

- **CNAME записи** указывают на **домены** (например, `u58316196.wl197.sendgrid.net`)
- **A записи** указывают на **IP адреса** (например, `76.76.21.21`)
- Для SendGrid нужны **CNAME записи**, а не A записи!

---

Сообщите:
1. Какой тип записи для `em5788` в Spaceship? (CNAME или A?)
2. Что показывает What's My DNS для CNAME типа?

Это поможет точно определить проблему.
