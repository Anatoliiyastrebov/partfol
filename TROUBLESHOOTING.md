# Устранение неполадок формы обратной связи

## Проверка работы формы

### 1. Проверьте, запущен ли backend сервер

```bash
cd server
npm start
```

Сервер должен запуститься на порту 3001 (или другом, указанном в `.env`).

### 2. Проверьте настройки в `.env`

Убедитесь, что файл `server/.env` содержит:

```env
PORT=3001
FRONTEND_URL=http://localhost:8000
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш_email@gmail.com
SMTP_PASS=ваш_пароль_приложения
EMAIL_TO=ваш_email@gmail.com
```

### 3. Проверьте консоль браузера

Откройте DevTools (F12) и проверьте:
- Есть ли ошибки в консоли
- Какой URL используется для запросов
- Какой ответ приходит от сервера

### 4. Проверьте консоль сервера

В терминале, где запущен сервер, должны быть логи:
- `📧 Получен запрос на отправку сообщения`
- `📝 Полученные данные: ...`
- `✅ Email успешно отправлен!` или ошибки

### 5. Тестирование через curl

```bash
curl -X POST http://localhost:3001/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "message": "Test message"
  }'
```

### 6. Частые проблемы

#### Проблема: CORS ошибка
**Решение:** Убедитесь, что `FRONTEND_URL` в `.env` соответствует URL, на котором открыт сайт.

#### Проблема: "Ошибка валидации данных"
**Решение:** Проверьте, что все поля заполнены правильно:
- Имя: минимум 2 символа
- Email: корректный формат
- Сообщение: минимум 5 символов

#### Проблема: "Ошибка подключения к SMTP"
**Решение:** 
- Проверьте правильность SMTP настроек
- Убедитесь, что используете пароль приложения Gmail (не обычный пароль)
- Проверьте, что включена двухфакторная аутентификация

#### Проблема: Сервер не отвечает
**Решение:**
- Проверьте, запущен ли сервер: `lsof -ti:3001`
- Проверьте порт в `.env`
- Убедитесь, что порт не занят другим процессом

### 7. Проверка работы формы

1. Откройте сайт в браузере
2. Откройте DevTools (F12) → вкладка Console
3. Заполните форму и отправьте
4. Проверьте логи в консоли браузера и сервера

### 8. Логи для отладки

В `script.js` добавлены подробные логи:
- `🔗 API Base URL:` - показывает, какой URL используется
- `📤 Отправка запроса на:` - URL запроса
- `📝 Тело запроса:` - данные формы
- `📥 Статус ответа:` - HTTP статус
- `✅ Ответ сервера:` - успешный ответ
- `❌ Ошибка:` - ошибки

В `server.js` также есть подробные логи всех операций.
