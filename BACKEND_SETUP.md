# 🚀 Быстрая настройка Backend

## Команды для консоли

### 1. Инициализация проекта и установка зависимостей

```bash
cd server
npm install
```

### 2. Создание файла конфигурации

```bash
cp .env.example .env
```

### 3. Настройка .env файла

Откройте файл `.env` и заполните следующие данные:

```env
# SMTP настройки для Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш.email@gmail.com
SMTP_PASS=ваш_пароль_приложения_16_символов
EMAIL_TO=ваш.email@gmail.com

# Настройки сервера
PORT=3000
FRONTEND_URL=http://localhost:8000
```

### 4. Запуск сервера

```bash
npm start
```

## 📝 Что нужно вставить вручную в .env

### Для Gmail:

1. **SMTP_HOST**: `smtp.gmail.com` (уже указано)
2. **SMTP_PORT**: `587` (уже указано)
3. **SMTP_USER**: Ваш Gmail адрес (например: `myemail@gmail.com`)
4. **SMTP_PASS**: Пароль приложения (см. инструкцию ниже)
5. **EMAIL_TO**: Ваш Gmail адрес, куда будут приходить сообщения

### Как получить пароль приложения для Gmail:

1. Включите двухфакторную аутентификацию в Google аккаунте
2. Перейдите: https://myaccount.google.com/apppasswords
3. Выберите "Почта" → "Другое устройство"
4. Введите название: "Portfolio Backend"
5. Скопируйте 16-символьный пароль
6. Вставьте его в `SMTP_PASS` в файле `.env`

## ✅ Проверка работы

### 1. Проверка здоровья сервера:

```bash
curl http://localhost:3000/health
```

### 2. Тестовая отправка сообщения:

```bash
curl -X POST http://localhost:3000/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тест",
    "email": "test@example.com",
    "message": "Тестовое сообщение"
  }'
```

## 🔧 Запуск фронтенда

В другом терминале (пока backend работает):

```bash
# Из корня проекта
python3 -m http.server 8000
```

Откройте в браузере: http://localhost:8000

## ⚠️ Частые ошибки

1. **"Ошибка подключения к SMTP"** → Проверьте правильность пароля приложения
2. **"CORS error"** → Убедитесь, что `FRONTEND_URL=http://localhost:8000` в `.env`
3. **"Cannot find module"** → Выполните `npm install` в папке `server`

Подробная документация: `server/README.md`
