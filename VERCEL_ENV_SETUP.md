# Настройка переменных окружения в Vercel

## Важно! 
Для работы формы на Vercel нужно настроить переменные окружения в панели Vercel.

## Шаги настройки:

### 1. Войдите в Vercel Dashboard
https://vercel.com/dashboard

### 2. Выберите ваш проект

### 3. Перейдите в Settings → Environment Variables

### 4. Добавьте следующие переменные:

#### SMTP_HOST
```
Значение: smtp.gmail.com
Environment: Production, Preview, Development (отметьте все)
```

#### SMTP_PORT
```
Значение: 587
Environment: Production, Preview, Development (отметьте все)
```

#### SMTP_USER
```
Значение: ваш_email@gmail.com
Environment: Production, Preview, Development (отметьте все)
```

#### SMTP_PASS
```
Значение: ваш_пароль_приложения_gmail
Environment: Production, Preview, Development (отметьте все)
```

#### EMAIL_TO
```
Значение: ваш_email@gmail.com (куда приходят письма)
Environment: Production, Preview, Development (отметьте все)
```

### 5. После добавления переменных

**ВАЖНО:** Нужно сделать новый деплой, чтобы переменные вступили в силу!

1. Перейдите в Deployments
2. Нажмите на три точки (⋯) у последнего деплоя
3. Выберите "Redeploy"
4. Или сделайте новый коммит и push в GitHub

### 6. Проверка работы

После редеплоя:
1. Откройте ваш сайт на Vercel
2. Откройте консоль браузера (F12)
3. Заполните и отправьте форму
4. Проверьте логи в Vercel Dashboard → Functions → contact

## Как получить пароль приложения Gmail:

1. Включите двухфакторную аутентификацию:
   - https://myaccount.google.com/security
   - Включите "Двухэтапная аутентификация"

2. Создайте пароль приложения:
   - https://myaccount.google.com/apppasswords
   - Выберите "Почта" и "Другое устройство"
   - Введите название: "Vercel Portfolio"
   - Скопируйте 16-значный пароль
   - Используйте его в `SMTP_PASS`

## Проверка через Vercel CLI:

```bash
# Просмотр переменных
vercel env ls

# Добавление переменной
vercel env add SMTP_HOST production
# Введите значение: smtp.gmail.com
```

## Troubleshooting:

### Проблема: "Сервер не настроен"
**Решение:** Убедитесь, что все переменные добавлены и проект передеплоен

### Проблема: "Ошибка при отправке email"
**Решение:**
- Проверьте правильность SMTP настроек
- Убедитесь, что используете пароль приложения (не обычный пароль)
- Проверьте логи в Vercel Dashboard → Functions

### Проблема: Переменные не работают
**Решение:**
- Убедитесь, что переменные добавлены для всех окружений (Production, Preview, Development)
- Сделайте новый деплой после добавления переменных
- Проверьте, что переменные не имеют лишних пробелов

## Логи в Vercel:

1. Зайдите в Vercel Dashboard
2. Выберите проект
3. Перейдите в Functions
4. Выберите функцию `contact`
5. Просмотрите логи выполнения

Там вы увидите:
- `📧 Получен запрос на отправку сообщения`
- `📝 Полученные данные: ...`
- `✅ Email успешно отправлен!` или ошибки
